let pathname = location.pathname.slice(1);
const pageSet = {
        bottomFilter: {
          hide_autoload: ["settings", "database"],
          turn_autoload: ["tags"],
          
          hide_filter: [],
          turn_filter: ["settings", "database"],
        },
      };